<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%= full_title(yield(:title)) %></a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="active">
          <a href="/"><%= icon "home" %> Главная</a>
        </li>
        <li>
          <a href="/settings"><%= icon "gear" %> Настройки</a>
        </li>
        <% if user_signed_in? %>
        <li class="dropdown">
          <a class="dropdown-toggle" aria-expanded="false" role="button" data-toggle="dropdown">
            Аккаунт: <%= current_user.name %>
            <span class="caret"></span>
          </a>
          <div class="uk-dropdown uk-dropdown-navbar">
            <ul class="uk-nav uk-nav-navbar">
              <li>
                <%= link_to current_user do %>
                <i class="uk-icon-desktop"></i> Открыть профиль
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, :method => :delete do %>
                <i class="uk-icon-sign-out"></i> Выйти из системы
                <% end %>
              </li>
            </ul>
          </div>
        </li>
        <% end %>
        <li>
          <a href="/help"><%= icon "info-circle" %> Помощь</a>
        </li>
      </ul>
      <form class="navbar-form navbar-right" role="search">
        <div class="input-group">
          <input class="form-control typeahead" type="text" placeholder="Поиск...">
          <span class="input-group-btn">
            <button type="submit" class="btn btn-primary active"><%= icon "search" %></button>
          </span>
        </div>
      </form>
    </div>
  </div>
</nav>
