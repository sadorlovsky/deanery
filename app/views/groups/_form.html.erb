<%= simple_form_for(@group) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :code %>
    <%= f.input :name %>
    <%= f.input : %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
