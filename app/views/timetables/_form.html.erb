<%= simple_form_for(@timetable) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :classroom %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
